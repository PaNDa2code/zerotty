name: Demo Workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest # This matches the label in your compose file
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from my self-hosted runner!"
